<Page x:Class="Entry_Data_Processing.Features.Dashboard.Views.DashboardPage"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
         Title="Dashboard">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Greeting}" FontSize="32" FontWeight="SemiBold" Margin="0,0,0,10" />
        <TextBlock Grid.Row="1" Text="Shortcut Hub" FontSize="20" FontWeight="Medium" Margin="0,20,0,15" />

        <WrapPanel Grid.Row="2" Orientation="Horizontal">

            <ui:CardAction Command="{Binding NavigateToRequestKodeCommand}" Width="280" Height="140" Margin="0,0,20,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <ui:SymbolIcon Symbol="Box24" FontSize="48" Foreground="{DynamicResource BrandYellow}" Grid.Column="0" Margin="0,0,15,0" />

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="Request Kode Barang Baru" FontWeight="SemiBold" FontSize="16" TextWrapping="Wrap" Margin="0,0,0,5" />
                        <TextBlock Text="Verifikasi, setujui, atau tolak permohonan dari portal" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" Margin="0,0,0,10" />

                        <StackPanel Orientation="Horizontal" Visibility="{Binding PendingRequestCount, Converter={StaticResource BooleanToVisibilityConverter}}">

                            <Border Background="#D97706" CornerRadius="10" Padding="8,2">
                                <TextBlock Text="{Binding PendingRequestCount}" Foreground="White" FontWeight="Bold" FontSize="12" />
                            </Border>
                            <TextBlock Text="Pending" Margin="8,0,0,0" VerticalAlignment="Center" FontSize="12" />
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </ui:CardAction>


            <ui:CardAction Width="280" Height="140" Margin="0,0,20,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <ui:SymbolIcon Symbol="Database24" FontSize="48" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Grid.Column="0" Margin="0,0,15,0" />

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="Master Data Ref" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,5" />
                        <TextBlock Text="(Coming Soon)" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    </StackPanel>
                </Grid>
            </ui:CardAction>

        </WrapPanel>
    </Grid>
</Page>
